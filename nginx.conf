#server {
#    listen 80;
#    listen [::]:80;
    #El server_name tengo que cambiarlo con mi ip : 200.234.230.76
    #server_name 200.234.230.76;
#    server_name 200.234.230.76 sportbuddies.es www.sportbuddies.es;
#    location / {
#       root /usr/share/nginx/html/browser;
#       index index.html;
#       try_files $uri $uri/ /index.html =404;
#    }
#}

#server {
    #listen 80;
    #server_name sportbuddies.es www.sportbuddies.es;

    #root /usr/share/nginx/html/browser;
    #index index.html;

    #location / {
    #    try_files $uri $uri/ /index.html;
    #}

#    error_page 404 /index.html;
#}

server {
    listen 80;
    server_name www.sportbuddies.es sportbuddies.es;

    location / {
        proxy_pass http://127.0.0.1:4200;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        # Agregar cabeceras necesarias
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Manejar errores 404 redirigiendo a Angular (SPA)
    error_page 404 /index.html;

    location = /index.html {
        root /usr/share/nginx/html/browser;  # Cambia a tu directorio estático si lo necesitas
    }
}